/**
 * External dependencies.
 */
import React from 'react';
import ReactDOM from 'react-dom';

const Calendash = () => (
    <h2>
        Hello from Calendash!
    </h2>
);

const root = document.getElementById( 'tec-calendash' );
ReactDOM.render(<Calendash />, root);
