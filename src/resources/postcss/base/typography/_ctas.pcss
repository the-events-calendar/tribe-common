.tribe-common {
	/* -------------------------------------------------------------------------
	 * CTA
	 * ------------------------------------------------------------------------- */

	.tribe-common-cta {
		@mixin desktop-body-3-bold;
		@mixin anchor-default;
	}

	.tribe-common-cta--alt {
		@mixin anchor-alt;
	}
}
